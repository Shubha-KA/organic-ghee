terraform {
  backend "azurerm" {
    # Configure by passing -backend-config values or environment variables
    # storage_account_name = "<storage_account>"
    # container_name       = "tfstate"
    # key                  = "terraform.tfstate"
  }
}
